server {
    listen 80;
    listen 443 ssl;

    ssl_certificate /certs/cert.pem;
    ssl_certificate_key /certs/key.pem;

    location / {
        root /var/www/html;
        index index.html;

        try_files $uri $uri/ /index.html;
    }
}